[
build
]
  functions = "functions"
  publish = "public"

[
functions
]
  node_bundler = "esbuild"

[[
redirects
]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[
dev
]
  functions = "functions"
  publish = "public"

[
build.environment
]
  NODE_VERSION = "18"